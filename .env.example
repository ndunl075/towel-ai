# Optional. Without a key the app falls back to the local heuristic extractor,
# so the demo still works offline.
ANTHROPIC_API_KEY=

# Optional override. Defaults to claude-opus-5.
NAPKIN_MODEL=claude-opus-5
